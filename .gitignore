__pycache__/
*.pyc
nohup.out

# PROPRIETARY — do not commit. The Lexical Aids PDF is all-rights-reserved
# (Gorgias Press), used here only under an attested IIT Goa partnership; it
# is NOT redistributable, so it must never enter a repo — least of all a
# public one you deploy from. It is not needed at runtime or for deployment:
# everything derived from it already lives in the compiled data/*.db. Keep
# it on your own machine for recompiling; never push it. (See
# data/lexical_aids/PROVENANCE.md, DECISIONS.md №27.)
data/lexical_aids/*.pdf

# Machine-local SEDRA IV response cache — regenerated by fetch-vocalised.
data/cache/sedra_api/*.json
